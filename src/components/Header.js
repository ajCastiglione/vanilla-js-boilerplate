const Header = () => {
    const template = `
    <header>
     <h1>Parcel App</h1>
     <p>Simple boilerplate for projects that require more in-depth JavaScript functionality.</p>    
    </header>
    `;

    return template;
}

export default Header;